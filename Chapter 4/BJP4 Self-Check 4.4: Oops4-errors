/**
 *The following program contains 7 errors. Correct the errors and submit a working version of the program. 
 The corrected version of the program should produce the following output:
 
 a is the smallest! 
 */
 
public class Oops4 {
    public static void main(String[] args) {
        int a = 7, b = 42;
        int smallerVariable = minimum(a, b);
        if (smallerVariable == a) {
            System.out.println("a is the smallest!");
        }
    }

    /**
     * This method is used to determine which either parameter a or b is smaller than the other and then returns
     * the smaller variable
     * 
     * @param a an int value passed to be tested against b
     * @param b an int value passed to be tested against a
     * @return the smaller variable value
     */
    public static int minimum(int a, int b) {
        int smallerValue;
        if (a < b) {
            smallerValue = a;
        } else {
            smallerValue = b;
        }
        return smallerValue;
    }
}
