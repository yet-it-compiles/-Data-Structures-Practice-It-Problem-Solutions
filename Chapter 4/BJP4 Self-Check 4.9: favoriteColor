/**
 * The following code contains a bug. Examine the code and figure out the case(s) in which the code would behave 
 * incorrectly. Then correct the bug in the code.
 */

Scanner console = new Scanner(System.in);
System.out.print("What is your favorite color? ");
String name = console.nextLine();
if (name.equals("blue")) { // remember that strings are objects and that we need to use this way
    System.out.println("Mine, too!");
}
