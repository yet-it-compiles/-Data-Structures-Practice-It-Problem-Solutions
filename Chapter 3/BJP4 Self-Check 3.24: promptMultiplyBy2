import java.util.Scanner;  // Don't forget this! This imports *only* the Scanner class
// import.java.util.*; // imports everything from the util class

/**
 Write code to read an integer from the user, then print that number multiplied by 2. You may assume that the user
 types a valid integer. A sample run of the code would produce the following:

 Type an integer: 4
 4 times 2 = 8
 */
 
 Scanner userInput = new Scanner(System.in);
        System.out.print("Type an integer: ");
    int userEnteredNumber = userInput.nextInt();
        System.out.println(userEnteredNumber + " times 2 = " + (2*userEnteredNumber));
